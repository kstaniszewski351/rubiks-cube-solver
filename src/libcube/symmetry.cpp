#include "symmetry.h"
#include "coord_moves.h"
#include "defs.h"
#include "cubie_cube.h"

void Symmetry::generateSyms(CoordMoves& moves)
{
    for(int i = 0; i < 16; i++)
    {
        for(int UDSlice = 0; UDSlice < UDSLICE_PERM_COUNT; UDSlice++)
        {
            for(int edgeOri = 0; edgeOri < EDGE_ORI_COUNT; edgeOri++)
            {

            }
        }
    }
}